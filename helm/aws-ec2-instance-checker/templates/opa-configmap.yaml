apiVersion: v1
data:
  check_imdsv1.rego: |
    package ec2

    default match = false

    match {
        input.MetadataOptions.HttpTokens == "optional"
        input.PublicIpAddress != null
    }
kind: ConfigMap
metadata:
  creationTimestamp: null
  name: opa-policy
